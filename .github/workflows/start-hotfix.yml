name: (▶) Start Hotfix

on: workflow_dispatch

jobs:
  start-hotfix:
    uses: sitepark/github-project-workflow/.github/workflows/composer-start-hotfix.yml@release/1.x
    with:
      ref: ${{ github.ref }}

